launcher.py try to execute the parallel Schartz method
But, because of some issues with fenics while using MPI (can load .xml) launcher.py will not work.

To see the result, one can run almost-parallel.py which do the same job, but in sequential.